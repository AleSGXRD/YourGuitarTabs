<% var count=12; for(var i=12 * pos;i<tabs.length&&count>0;i++) { %>

    <a class="<%= i==0 + 12 * pos ?  'first' : i==tabs.length - 1 || (pos + 1) * 12 - 1==i ? 'last' : ''  %>"
        href="<%= tabs[i].direc%>">
        <%= i+1 %>- <%= tabs[i].description %>
    </a>

    <% count--; } %>